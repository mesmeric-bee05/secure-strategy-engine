# Security Findings — 2026-06-12

Re-scan after Track 5 (pgvector schema move) + Track 6 (function hardening).
**Connector scan (Wiz):** 0 findings.

| # | Scanner | Severity | Resource | Status | Next action |
|---|---------|----------|----------|--------|-------------|
| 1 | supabase_lov | error | `profiles` policy `profiles_authenticated_read` | OPEN | Tighten policy: split into own-profile (USING auth.uid()=id) + a curated public view excluding gender/age_band/region/ecdsa_public_key |
| 2 | supabase_lov | warn  | `skills` policy `skills_owner_read` (anon sees verified rows w/ observations) | OPEN | Restrict public read to non-PII column projection (server fn + service role for curated DTO) |
| 3 | supabase_lov | warn  | `webauthn_challenges` | OPEN | Add explicit `FOR ALL TO anon, authenticated USING(false) WITH CHECK(false)` to match audit_log / rate_limits |
| 4 | tanstack | warn | `supabase/functions/_shared/cors.ts` wildcard `*` + `authorization` | OPEN | Replace `*` with allowlisted origins; add `Vary: Origin` |
| 5 | tanstack | warn | edge fns trust client `x-user-id` header for audit | OPEN | Derive user id from verified JWT via `supabase.auth.getUser(token)` |
| 6 | supabase  | warn | `has_role` EXECUTE on authenticated | ACCEPTED | Required inside RLS policy expressions |
| 7 | supabase  | warn | `submit_attestation` EXECUTE on authenticated | ACCEPTED | Hardened in migration 2026-06-12 (rate limit, dup guard, length bounds, audit logging) |

## Posture deltas vs previous scan
- pgvector-in-public: **CLEARED** (moved to `extensions` schema).
- attestation email leak via verified skills: **CLEARED**.
- profiles public read: **CLEARED** (was anon; now authenticated-only — still too broad, see #1).
- credential_anchors public read: **CLEARED**.
- write-policy gaps on audit_log / fairness_audits / rate_limits / credential_anchors / attestations: **CLEARED**.

## Newly surfaced
- #1, #2 (sensitive-column projection): existed before but were masked by the louder `USING true` policies.
- #3 (webauthn_challenges deny-write): same hardening pattern as audit_log; one-line policy.
- #4, #5 (edge function CORS + forged user id): pre-existing, surfaced because edge functions are now scanned in this revision.

## Priority order for next sprint
1. **#1** — profiles policy split (highest severity).
2. **#5** — derive user id from JWT (audit integrity).
3. **#4** — CORS allowlist (defense-in-depth).
4. **#2** — skills column projection for anon.
5. **#3** — webauthn_challenges explicit deny-write.
